<%= form_for(@ability) do |f| %>
  <% if @ability.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ability.errors.count, "error") %> prohibited this ability from being saved:</h2>

      <ul>
      <% @ability.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :cost %><br />
    <%= f.text_field :cost %>
  </div>
  <div class="field">
    <%= f.label :effect %><br />
    <%= f.text_area :effect %>
  </div>
  <div class="field">
    <%= f.label :effect_code %><br />
    <%= f.text_area :effect_code %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
