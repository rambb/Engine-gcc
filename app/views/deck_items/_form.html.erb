<%= form_for(@deck_item) do |f| %>
  <% if @deck_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@deck_item.errors.count, "error") %> prohibited this deck_item from being saved:</h2>

      <ul>
      <% @deck_item.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :card_id %><br />
    <%= f.text_field :card_id %>
  </div>
  <div class="field">
    <%= f.label :deck_id %><br />
    <%= f.text_field :deck_id %>
  </div>
  <div class="field">
    <%= f.label :quantity %><br />
    <%= f.text_field :quantity %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
